{
  "name": "payment-service",
  "version": "1.0.0",
  "description": "Sample payment service for CI/CD",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "jest --watchAll=false"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "devDependencies": {
    "jest": "^25.0.0",
    "supertest": "^7.2.2"
  }
}
